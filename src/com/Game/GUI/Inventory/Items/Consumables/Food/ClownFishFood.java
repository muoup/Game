package com.Game.GUI.Inventory.Items.Consumables.Food;

public class ClownFishFood extends Food {

    public ClownFishFood(int id, String imageName, String name, String examineText, int maxStack) {
        super(id, imageName, name, examineText, maxStack, 25);
    }
}
