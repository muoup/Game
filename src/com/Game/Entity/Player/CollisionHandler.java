package com.Game.Entity.Player;

import com.Util.Math.Vector2;

public class CollisionHandler {
    /*
       New Plan: Create a collision map image and use that for collision instead
       It'll be easier and faster for the computer. For now it will nto exist.
     */
    public static boolean isFree(Vector2 position) {
        return true;
    }
}
